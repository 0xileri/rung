{
  "name": "limit-plus",
  "private": true,
  "version": "0.1.0",
  "description": "Limit+ — a capital-backed valuation market for PreStocks on Solana",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "node --test \"packages/sdk/test/**/*.test.ts\""
  },
  "type": "module"
}
