node_modules/
target/
.anchor/
dist/
.next/
.env
.env.local
test-ledger/
*.log

# Anchor build artifacts (the .so is rebuildable; the program keypair is kept below)
target/
# NOTE: target/deploy/rung-keypair.json is the program's identity and is deliberately NOT
# committed. Publishing it before the program is deployed would let someone else claim the
# address. Back it up outside the repo: losing it changes the program id, which strands the
# SOL in the buffer left by the failed devnet deploy.
